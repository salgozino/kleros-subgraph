{
  "name": "klerosboard",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ salgozino/klerosboard",
    "create-local": "graph create --node http://localhost:8020/ salgozino/klerosboard",
    "remove-local": "graph remove --node http://localhost:8020/ salgozino/klerosboard",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 salgozino/klerosboard"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "^0.20.0",
    "keytar": "^7.7.0",
    "typescript": "^4.2.2"
  }
}
